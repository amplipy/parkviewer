# Runtime dependencies for the ParkViewer app (Streamlit UI + core logic).
numpy
pillow
matplotlib
matplotlib-scalebar
python-pptx
streamlit>=1.36
streamlit-image-select
pywebview>=5.0

# Linux native window: pywebview uses Qt WebEngine there (mac/win have native
# WebKit/Cocoa and Edge WebView2 backends built into the OS).
PyQt6; sys_platform == "linux"
PyQt6-WebEngine; sys_platform == "linux"
qtpy; sys_platform == "linux"

# Packaging (used by scripts/build_* and CI)
pyinstaller>=6.10